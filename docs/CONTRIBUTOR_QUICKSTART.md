# Contributor quickstart

1. Fork and clone the repository.
2. Create a focused branch.
3. Run the site locally with `python -m http.server 8000`.
4. For tool changes, edit `data/tools.json` and run `python scripts/validate_tools.py`.
5. Open a pull request and link the relevant issue.

For full guidance, see the root `CONTRIBUTING.md` file.
