# GitHub Actions

- `source-monitor.yml` refreshes monitored AI signals on schedule.
- `validate-tools.yml` validates curated tool catalog changes.
- `validate-json.yml` verifies JSON data files remain parseable on relevant pull requests.
